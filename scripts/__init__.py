"""The character sets composed on top of a Betaflight OSD font.

`shapes` holds the drawing primitives and the measurements taken off a font;
the other modules each add one set of characters the source font does not have.
`bf2font.py` at the repository root is the entry point that assembles them.
"""
