[project]
name = "betaflight-designer-fonts"
version = "1.0.0"
description = "Convert Betaflight/MAX7456 .mcm OSD fonts into sliced PNGs and colour font files (OTF/TTF/WOFF/WOFF2)."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0-only" }
keywords = [
    "betaflight",
    "osd",
    "font",
    "fpv",
    "max7456",
    "mcm",
    "opentype",
    "color-font",
    "colr",
    "woff2",
]
dependencies = [
    "fonttools>=4.53",
    "brotli>=1.1",
    "pillow>=10.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1",
    "uharfbuzz>=0.39",
    "ruff>=0.16.7",
    "mypy>=1.14",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle
    "W",
    "F",    # pyflakes
    "I",    # import sorting
    "N",    # naming
    "UP",   # pyupgrade
    "B",    # bugbear
    "C4",   # comprehensions
    "SIM",  # simplify
    "PTH",  # use pathlib
    "RET",  # returns
    "RUF",
]

[tool.ruff.lint.per-file-ignores]
# These files are about letters and signs from other writing systems.
# Characters that look like Latin ones are the subject matter, not a typo.
"scripts/cyrillic.py" = ["RUF001", "RUF002", "RUF003"]
"scripts/greek.py" = ["RUF001", "RUF002", "RUF003"]
"scripts/punctuation.py" = ["RUF001", "RUF002", "RUF003"]
"tests/test_cyrillic.py" = ["RUF001", "RUF002", "RUF003"]
"tests/test_sets.py" = ["RUF001", "RUF002", "RUF003"]
"tools/specimen.py" = ["RUF001", "RUF002", "RUF003"]
"tools/symbol_index.py" = ["RUF001", "RUF002", "RUF003"]

[tool.mypy]
python_version = "3.10"
files = ["bf2font.py", "scripts", "tools", "tests"]
mypy_path = "tests"
warn_unused_ignores = true
warn_redundant_casts = true
warn_unreachable = true
disallow_untyped_defs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
